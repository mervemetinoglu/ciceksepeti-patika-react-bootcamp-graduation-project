import React from "react";
import { WrapperContainer } from "./scContainer";

const Container = ({ children }) => {
  return <WrapperContainer>{children}</WrapperContainer>;
};

export default Container;
