import React from "react";
import { StyledLoader, LoaderWheel } from "./scSpinner";

const Loader = () => {
  return (
    <StyledLoader>
      <LoaderWheel />
    </StyledLoader>
  );
};

export default Loader;
